{
  "name": "inventario-frenospala",
  "version": "1.0.0",
  "description": "Inventario Frenos Pala — App Android",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "sync":  "node build.js && npx cap sync android",
    "open":  "npx cap open android",
    "apk":   "node build.js && npx cap sync android && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core":    "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
